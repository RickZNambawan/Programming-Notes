** IN GIT BASH **

* ssh-keygen -t rsa -b 4096 -C "email address here"
* (ENTER)
* (ENTER)
* (ENTER)

*eval $(ssh-agent -s)

* ssh-add -/.ssh/id_rsa

* cli[ < -/.ssh/id_rsa.pub

go to settings
*ssh tab
*ctrl + v